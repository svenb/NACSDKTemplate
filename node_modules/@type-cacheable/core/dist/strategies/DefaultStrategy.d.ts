import { CacheStrategy, CacheStrategyContext } from '../interfaces';
export declare class DefaultStrategy implements CacheStrategy {
    private pendingCacheRequestMap;
    private pendingMethodCallMap;
    private findCachedValue;
    handle(context: CacheStrategyContext): Promise<any>;
}
