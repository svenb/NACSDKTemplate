import { CacheClearStrategy, CacheClearStrategyContext } from '../interfaces';
export declare class DefaultClearStrategy implements CacheClearStrategy {
    private handleDelete;
    handle(context: CacheClearStrategyContext): Promise<any>;
}
