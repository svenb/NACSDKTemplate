import { CacheClient, CacheManagerOptions } from './interfaces';
export default class CacheManager {
    client: CacheClient | null;
    fallbackClient: CacheClient | null;
    options: CacheManagerOptions;
    setClient(client: CacheClient): void;
    setFallbackClient(client: CacheClient): void;
    setOptions(options: CacheManagerOptions): void;
}
